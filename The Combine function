# The Combine function
# def mean define. These are the inputs(c, d, e). the () brackets serves as the parameters
def combine(c, d, e):  
  outcome = e + d * e # this meant to be the body of the statment for the program.
  return outcome
#return will be the output for the program
print(combine(15, 30, 45))
# print with the numbers will be are argument.
